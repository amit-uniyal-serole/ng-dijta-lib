import { InputMaskPipe } from './input-mask.pipe';

describe('InputMaskPipe', () => {
    it('create an instance', () => {
        const pipe = new InputMaskPipe();
        expect(pipe).toBeTruthy();
    });
});