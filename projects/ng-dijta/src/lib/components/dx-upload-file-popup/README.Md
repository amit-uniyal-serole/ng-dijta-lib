## Module
`DxUploadFilePopupModule`

### defaultFileSize in bytes 
> pass default file size to upload-file-popup if required ,When the upload file exceed it's size user cannot proceed to upload that file.

### fileFormat 
> default fileFormat is **video/*** which accepts all video formats,If Required we can pass any another formats`(Ex:image/jpg,image/png,.pdf)`